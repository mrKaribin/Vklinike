<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/profile_subscription_add"
    android:focusable="true"
    android:clickable="true"
    android:padding="@dimen/margin_small"
    style="@style/ButtonRounded"
    tools:context=".ui.profile.doctor.ProfileSubscriptionAddFragment">

    <ImageView
        android:layout_width="@dimen/profile_subscription_add_icon_size"
        android:layout_height="@dimen/profile_subscription_add_icon_size"
        android:layout_gravity="center"
        android:src="@drawable/icon_add"/>

</FrameLayout>